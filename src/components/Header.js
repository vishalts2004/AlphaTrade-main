import React from 'react';
import './Header.css'; // Ensure you have corresponding CSS file

const Header = () => {
    return (
        <header className="header">
            <div className="logo">
                <h1 className="header-title">AlphaTrade</h1>
            </div>
        </header>
    );
};

export default Header;
